# Program-5
Created by Joshua Veal and Joel Peters. 
Program displays and allows a user to build karts from mario kart then saves them and allows for races.

In order to install, create new project, in src folder > main > java, place module-info, then > mario > kart > program5 should contain all other .java files
  .fxml files depart at main > resources > mario > kart > program5 and this contains the .fxml files
